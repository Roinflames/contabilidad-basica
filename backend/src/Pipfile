[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
djangorestframework = "~=3.0"
psycopg = {extras = ["binary"], version = "~=3.0"}
django-environ = "~=0.11"
django = "~=5.0"

[dev-packages]

[requires]
python_version = "3.12"
